# 空状态

用于显示空状态的组件。

## 关键属性

- `title`: 设置空状态的标题。
- `description`: 设置空状态的描述。
- `icon`: 设置空状态的图标。
- `avatar`: 设置空状态的头像。
- `actions`: 向空状态添加一些 [Button](/docs/components/button) 操作。
- `variant`: 更改空状态的变体。
- `size`: 更改空状态的大小。

## 用法

```vue
<UEmpty
  <!-- props here -->
/>
```
