# 表单字段

为表单元素提供验证和错误处理的包装器。

## 关键属性

- `label`: 为表单控件设置标签。
- `description`: 在标签下方提供附加信息。
- `hint`: 在标签旁边显示提示消息。
- `help`: 在表单控件下方显示帮助消息。
- `error`: 在表单控件下方显示错误消息。
- `size`: 更改表单字段的大小，`size` 将代理到表单控件。

## 用法

```vue
<UFormField
  <!-- props here -->
/>
```
