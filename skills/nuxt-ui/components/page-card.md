# 页面卡片

预样式的卡片组件，显示标题、描述和可选链接。

## 关键属性

- `title`: 设置卡片的标题。
- `description`: 设置卡片的描述。
- `icon`: 设置卡片的图标。
- `variant`: 更改卡片的样式。
- `orientation`: 更改默认插槽的方向。
- `reverse`: 反转默认插槽的方向。

## 用法

```vue
<UPageCard
  <!-- props here -->
/>
```
