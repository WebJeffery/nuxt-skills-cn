# 页面 CTA

用于在您的页面中显示的行动号召部分。

## 关键属性

- `title`: 设置 CTA 的标题。
- `description`: 设置 CTA 的描述。
- `links`: 在描述下方显示 [Button](/docs/components/button) 列表。
- `variant`: 更改 CTA 的样式。
- `orientation`: 更改默认插槽的方向。
- `reverse`: 反转默认插槽的方向。

## 用法

```vue
<UPageCta
  <!-- props here -->
/>
```
